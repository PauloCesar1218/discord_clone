import styled from "styled-components";

export const NotificationPinContainer = styled.div`
    border: 3px solid var(--tertiary);
    background-color: var(--notification);
    border-radius: 100%;
    padding: 3px 6px;
    color: var(--white);
    font-size: 12px;
    display: flex;
    justify-content: center;
    align-items: center;
`
